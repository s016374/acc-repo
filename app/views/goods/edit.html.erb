<h1>Editing Good</h1>

<%= form_for(@good, url: order_good_path) do |f| %>
  <% if @good.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@good.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @good.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Show', order_good_path(@good) %> |
<%= link_to 'Back', order_goods_path %>
